/** Settings — keybinding remapper, volume controls. */
export default function Settings() {
  return (
    <div style={{ paddingTop: "2rem" }}>
      <h2>Settings</h2>
      <p style={{ color: "#888" }}>Settings coming soon</p>
    </div>
  );
}
